name: Pytest Python Testing
on: 
  push:
    branches:
      - '*'
      - '!master'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r Server/requirement.txt
      - name: Test with pytest
        run: |
            pytest --cov-branch --junitxml=pytest.xml --covreport=term-missing:skip.covered --cov --html=report.html        
